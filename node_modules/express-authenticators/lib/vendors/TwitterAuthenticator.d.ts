import OAuth, { IOAuthTokenPayload } from '../oauth/OAuth';
import { IOAuthProfileFetcher } from '../OAuthCommon';
export default class TwitterAuthenticator extends OAuth implements IOAuthProfileFetcher<IOAuthTokenPayload> {
    constructor(config: {
        clientID: string;
        clientSecret: string;
        redirectUri: string;
    });
    fetchProfile(tokenPayload: IOAuthTokenPayload): Promise<{
        id: any;
        raw: any;
        avatar: any;
        first: any;
        email: any;
        emailVerified: boolean;
    }>;
}
