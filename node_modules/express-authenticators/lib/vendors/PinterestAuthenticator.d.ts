import OAuth2 from '../oauth2/OAuth2';
import { IOAuthProfileFetcher } from '../OAuthCommon';
interface IPinterestTokenPayload {
    status: 'success';
    message: 'ok';
    code: 0;
    data: {
        access_token: string;
        expires_at: number;
        consumer_id: string | number;
        token_type: 'bearer';
        authorized: true;
        scope: string;
    };
}
export default class PinterestAuthenticator extends OAuth2<IPinterestTokenPayload> implements IOAuthProfileFetcher<IPinterestTokenPayload> {
    fetchProfile: ({ data: { access_token } }: IPinterestTokenPayload) => Promise<{
        id: any;
        first: any;
        last: any;
        raw: any;
    }>;
    constructor(options: {
        clientID: string;
        clientSecret: string;
        redirectUri: string;
        scope?: string;
    });
}
export {};
