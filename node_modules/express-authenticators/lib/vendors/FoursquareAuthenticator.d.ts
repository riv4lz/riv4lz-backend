import OAuth2 from '../oauth2/OAuth2';
import { IOAuthProfileFetcher } from '../OAuthCommon';
interface IFoursquareTokenPayload {
    access_token: string;
}
export default class FoursquareAuthenticator extends OAuth2<IFoursquareTokenPayload> implements IOAuthProfileFetcher<IFoursquareTokenPayload> {
    fetchProfile: ({ access_token }: IFoursquareTokenPayload) => Promise<{
        id: any;
        first: any;
        last: any;
        email: any;
        emailVerified: boolean;
        avatar: string;
        raw: any;
    }>;
    constructor(options: {
        clientID: string;
        clientSecret: string;
        redirectUri: string;
        scope?: string;
    });
}
export {};
