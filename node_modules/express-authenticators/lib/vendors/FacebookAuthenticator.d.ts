import OAuth2 from '../oauth2/OAuth2';
import { IOAuthProfileFetcher } from '../OAuthCommon';
interface IFacebookTokenPayload {
    access_token: string;
    token_type: string;
    expires_in: number;
}
export default class FacebookAuthenticator extends OAuth2<IFacebookTokenPayload> implements IOAuthProfileFetcher<IFacebookTokenPayload> {
    fetchProfile: ({ access_token }: IFacebookTokenPayload, fields?: string[]) => Promise<{
        id: any;
        first: any;
        last: any;
        email: any;
        emailVerified: boolean;
        avatar: string;
        raw: any;
    }>;
    constructor(options: {
        clientID: string;
        clientSecret: string;
        redirectUri: string;
        scope?: string;
    });
}
export {};
