import OAuth2 from '../oauth2/OAuth2';
import { IOAuthProfileFetcher } from '../OAuthCommon';
interface IInstagramTokenPayload {
    access_token: string;
    user_id: string;
}
export default class InstagramAuthenticator extends OAuth2<IInstagramTokenPayload> implements IOAuthProfileFetcher<IInstagramTokenPayload> {
    fetchProfile: ({ access_token }: IInstagramTokenPayload, fields?: string[]) => Promise<{
        id: any;
        first: any;
        avatar: any;
        raw: {
            profile: any;
            graphProfile: any;
        };
    }>;
    constructor(options: {
        clientID: string;
        clientSecret: string;
        redirectUri: string;
        scope?: string;
    });
}
export {};
