import OAuth2 from '../oauth2/OAuth2';
import { IOAuthProfileFetcher } from '../OAuthCommon';
interface ILinkedInTokenPayload {
    access_token: string;
    expires_in: number;
}
export default class LinkedInAuthenticator extends OAuth2<ILinkedInTokenPayload> implements IOAuthProfileFetcher<ILinkedInTokenPayload> {
    fetchProfile: ({ access_token }: ILinkedInTokenPayload) => Promise<{
        id: any;
        first: any;
        last: any;
        raw: any;
    }>;
    constructor(options: {
        clientID: string;
        clientSecret: string;
        redirectUri: string;
        scope?: string;
    });
}
export {};
