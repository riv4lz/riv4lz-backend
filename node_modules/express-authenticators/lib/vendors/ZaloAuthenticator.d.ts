import OAuth2 from '../oauth2/OAuth2';
import { IOAuthProfileFetcher } from '../OAuthCommon';
interface IZaloTokenPayload {
    access_token: string;
    refresh_token: string;
    expires_in: string;
}
export default class ZaloAuthenticator extends OAuth2<IZaloTokenPayload> implements IOAuthProfileFetcher<IZaloTokenPayload> {
    private childConfig;
    fetchProfile: ({ access_token }: IZaloTokenPayload, fields?: string[]) => Promise<{
        id: any;
        first: any;
        avatar: any;
        raw: any;
    }>;
    constructor(childConfig: {
        clientID: string;
        clientSecret: string;
        redirectUri: string;
        scope?: string;
    });
    refreshAccessToken(refreshToken: string): Promise<IZaloTokenPayload>;
}
export {};
