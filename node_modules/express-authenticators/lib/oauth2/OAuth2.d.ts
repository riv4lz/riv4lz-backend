import { IOAuthCommon, IPopSession, IStoreSession } from '../OAuthCommon';
export declare const enum TokenRequestMethod {
    GET = "GET",
    POST = "POST",
    PUT = "PUT"
}
export default class OAuth2<T> implements IOAuthCommon<T> {
    #private;
    private config;
    private options;
    constructor(config: {
        consentURL: string;
        tokenURL: string;
        clientID: string;
        clientSecret: string;
        redirectUri: string;
        scope?: string;
    }, options: {
        ignoreGrantType: boolean;
        tokenRequestMethod: TokenRequestMethod;
        includeStateInAccessToken: boolean;
        enablePKCE: boolean;
        clientIDQueryName?: string;
        secretHeaderName?: string;
    });
    callback({ pop }: IPopSession, rawQuery: string): Promise<T>;
    authenticate({ store }: IStoreSession): Promise<string>;
}
